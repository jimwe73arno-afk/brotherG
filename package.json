cd ~/Desktop/brotherG-EV

# 刪除舊的
rm -f package.json

# 創建新的（確保格式正確）
cat > package.json << 'EOF'
{
  "name": "brotherg-ev",
  "version": "1.0.0",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1"
  }
}
EOF

# 確認內容正確
cat package.json

# 重新提交
git add package.json
git commit -m "fix: recreate package.json with correct format"
git push
